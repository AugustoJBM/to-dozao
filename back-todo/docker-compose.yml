services:
  postgres:
    image: postgres:16
    container_name: todozao-postgres
    environment:
      POSTGRES_DB: todozao
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
